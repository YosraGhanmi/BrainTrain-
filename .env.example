# Admin dashboard (Ctrl+Shift+A) — set both before deploying.
# Without these, the app falls back to insecure defaults for local dev only.
ADMIN_PASSWORD=change-me
ADMIN_SESSION_SECRET=change-me-too

# Parent Portal + Child Follow-Up System
# ---------------------------------------------------------------------------

# PostgreSQL (see docker-compose.yml for local dev: `docker compose up -d`)
DATABASE_URL=postgresql://braintrain:braintrain@localhost:5433/braintrain

# Signs parent/teacher session cookies. Set a long random value before deploying.
PORTAL_SESSION_SECRET=change-me-portal-secret

# Stripe — test-mode keys are fine for local dev (https://dashboard.stripe.com/test/apikeys).
# STRIPE_WEBHOOK_SECRET comes from `stripe listen --forward-to localhost:3000/api/stripe/webhook`.
STRIPE_SECRET_KEY=sk_test_change-me
STRIPE_WEBHOOK_SECRET=whsec_change-me

# SMS provider abstraction — "twilio" is the only implementation for now, but
# lib/sms/index.ts can dispatch on this value to swap providers later.
SMS_PROVIDER=twilio
TWILIO_ACCOUNT_SID=change-me
TWILIO_AUTH_TOKEN=change-me
TWILIO_FROM_NUMBER=+15005550006

# Shared secret required in the `x-cron-secret` header when an external
# scheduler calls /api/cron/payment-reminders.
CRON_SECRET=change-me-cron-secret
