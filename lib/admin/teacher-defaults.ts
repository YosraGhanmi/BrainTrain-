// Shared between the create-teacher server action and the admin Teachers
// page (which displays it after creation) — not a 'use server' file since
// those can only export async functions.
export const DEFAULT_TEACHER_PASSWORD = 'braintrain@TEACHER';
